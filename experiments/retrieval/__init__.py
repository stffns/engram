"""Public retrieval benchmarks.

These ask: *does the substrate find what's there?* The answer mostly
depends on vstash's chunker, embedder, and hybrid weights — merken's
loop barely participates. Useful for absolute positioning, not for
validating decision policies.

For benchmarks that measure the *loop*, see ``experiments/loop_quality/``.
"""
