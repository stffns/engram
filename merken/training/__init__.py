"""Training-data pipelines for merken's optional models.

Modules here build datasets for downstream classifier / midloop
training. They are NOT part of the runtime path -- importing
``merken`` does not import this subpackage. CLI entry points and
tests do.
"""
